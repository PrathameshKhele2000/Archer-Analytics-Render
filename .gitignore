# Secrets / local config — never commit
.env
*.local

# Dependencies & build output
node_modules/
dist/
*.tsbuildinfo

# Logs & OS cruft
*.log
.DS_Store

# Local database dumps (large; not source)
azure_db_dump.sql
backup.sql
